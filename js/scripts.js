function calculaArea(base, altura) {
    return base * altura;
}
// Exemplo de uso da função:
let areaRetangulo = calculaArea(5, 10); // Chamando a função e armazenando o resultado em uma variável
console.log("A área do retângulo é:", areaRetangulo); // Imprimindo o resultado no console